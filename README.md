
# OptimAIzer API

Une API pour recevoir une configuration système (CPU, RAM, GPU, stockage, etc.)
et retourner une analyse structurée en français québécois.

Fait pour être connecté à un GPT personnalisé (via schéma OpenAPI).
